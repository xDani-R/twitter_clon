<!DOCTYPE html>
<html>
  <head>
    <title>TwitterClone</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <!-- Barra de navegación -->
  <nav class="navbar navbar-expand-lg navbar-light" style="background: linear-gradient(to right, #4b6cb7, #182848);">
    <div class="container-fluid">
      <a class="navbar-brand text-white" href="<%= root_path %>">
        <img src="https://img.icons8.com/ios-filled/50/ffffff/twitter.png" alt="Logo" width="30" height="30" class="d-inline-block align-text-top">
        TwitterClone
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= link_to "Inicio", root_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to "Tweets", tweets_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to "Nuevo Tweet", new_tweet_path, class: "btn btn-light btn-sm ms-2" %>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Contenedor principal -->
  <div class="container mt-4">
    <%= yield %>
  </div>

  <!-- Pie de página -->
  <footer class="bg-dark text-white text-center py-3 mt-4">
    <p>&copy; 2024 TwitterClone. Todos los derechos reservados.</p>
  </footer>
  </body>
</html>

